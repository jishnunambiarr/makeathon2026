# Agent backend (hackathon)

- **`backend/agent-server`** — ElevenLabs token minting + optional HTTP tools. See `backend/agent-server/README.md`.
- **`backend/moodle-playwright`** — Playwright scripts for **moodle.tum.de** (session save + fetch resource link). See `backend/moodle-playwright/README.md`.

